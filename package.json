{
  "name": "scraper-ps",
  "version": "1.0.0",
  "description": "PS5 Pro Availability Scraper",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "bash build.sh",
    "build:production": "REACT_APP_API_URL=https://your-backend-url.vercel.app npm run build",
    "deploy": "npm run build && git add dist && git commit -m 'Deploy to GitHub Pages' && git push origin gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "./",
  "private": true,
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5"
  },
  "devDependencies": {}
}
